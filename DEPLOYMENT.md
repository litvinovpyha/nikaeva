# Инструкции по развертыванию

## 🚀 Разработка (Development)

### Локальная разработка:
```bash
# Запуск серверов
start-local.bat

# Или вручную:
php artisan serve
npm run dev
```

### Сетевая разработка (доступ с других устройств):
```bash
# Запуск серверов на IP 192.168.0.16
start-servers.bat

# Или вручную:
php artisan serve --host=192.168.0.16 --port=8000
npm run dev -- --host 192.168.0.16
```

## 🌐 Продакшен (Production)

### Автоматическая сборка:
```bash
# Сборка для продакшена
build-production.bat
```

### Ручная сборка:
```bash
# 1. Установка зависимостей
npm install

# 2. Сборка ресурсов
npm run build

# 3. Очистка кешей
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan cache:clear

# 4. Оптимизация для продакшена
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

## 🔒 Content Security Policy (CSP)

### Автоматическое определение окружения:
- **Разработка**: CSP разрешает загрузку с Vite сервера (порт 5173)
- **Продакшен**: CSP использует только статические ресурсы

### Определение окружения:
- По переменной `APP_ENV` (local/development = разработка)
- По хосту (localhost, 127.0.0.1, 192.168.x.x, 10.x.x.x, 172.x.x.x = разработка)
- Все остальные хосты = продакшен

## 📁 Структура файлов

### Батники для разработки:
- `start-local.bat` - локальная разработка (127.0.0.1)
- `start-servers.bat` - сетевая разработка (192.168.0.16)
- `stop-servers.bat` - остановка всех серверов

### Батники для продакшена:
- `build-production.bat` - сборка для продакшена
- `clear-cache.bat` - очистка кешей

### Утилиты:
- `fix-encoding.bat` - исправление кодировки терминала

## ⚙️ Настройка сервера

### Nginx/Apache:
```bash
# Убедитесь, что:
# 1. APP_ENV=production в .env
# 2. npm run build выполнен
# 3. Права на запись в storage/ и bootstrap/cache/
```

### Переменные окружения:
```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://yourdomain.com
```

## 🔧 Устранение неполадок

### Стили не загружаются:
1. Проверьте, что Vite запущен (`npm run dev`)
2. Очистите кеш: `php artisan config:clear`
3. Проверьте CSP в DevTools браузера

### CSP ошибки:
- В разработке: CSP автоматически разрешает Vite
- В продакшене: используйте `npm run build`

### Кракозабры в терминале:
- Запустите `fix-encoding.bat`
- Или вручную: `chcp 65001`
